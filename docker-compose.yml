version: "3.9"

services:
  web:
    build: .
    container_name: zkp_flask_app
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./database:/app/database
    env_file:
      - .env